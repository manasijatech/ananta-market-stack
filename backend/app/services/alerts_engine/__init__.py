"""Alert engine v2 helpers.

The public FastAPI service still lives in ``app.services.alerts`` for backward
compatibility. New alert runtime logic is kept here so it can evolve without
turning the compatibility service into a monolith.
"""

